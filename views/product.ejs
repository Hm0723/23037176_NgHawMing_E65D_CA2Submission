<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Pc Shop App</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-black navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Home</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown">Category</a>
            <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="/pc">PC</a></li>
                <li><a class="dropdown-item" href="/laptop">Laptop</a></li>
                <li><a class="dropdown-item" href="/accessory">Accessory</a></li>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-link text-white" role="button">
                    <a class="nav-link" href="/contact">Contact Us</a>
                </li>
                <li class="nav-link text-white" role="button">
                    <a class="nav-link" href="/about">About Us</a>
                </li>
                <li class="nav-link text-white" role="button">
                    <a class="nav-link" href="/addProduct">Add Product</a>
                </li>
                
            </ul>
        </div>
        
    </nav>
    
    <div class="container">
        <br>
        <div class="text-center"><h2>Product Details</h2></div>
        <br>
        <% if (product) { %> <!-- Check if product data exists -->
            <div class="row">
                <div class="col-md-4">
                    <img src="<%= product.image %>" class="img-fluid" alt="<%= product.productName %>">
                </div>
                <div class="col-md-8">
                    <h2><%= product.productName %></h2>
                    <% 
                    const lines = product.description.split('\n'); //splits product.description into an array of lines using .split('\n'), Then, it iterates over each line using forEach, and for each line, it outputs it within <p> tags using the % line
                    lines.forEach(line => { %>              
                        <p><%= line %></p>
                    <% }); %>
                    <p><h2>Price:</h2> $<%= product.price %></p>

                    <a href="/editProduct/<%= product.productId %>" class="btn btn-warning">Edit</a>

                    <form action="/deleteProduct/<%= product.productId %>" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>

                </div>
            </div>
            
        <% } else { %>
            <p>No product found.</p>
        <% } %>
        <a href="/" class="btn btn-primary mt-3">Back</a>
    </div>
    
</body>
</html>
