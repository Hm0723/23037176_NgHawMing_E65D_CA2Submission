<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <style>
        .card {
            height: 100%;
        }
        .card-img-top {
            object-fit: contain;
            height: 275px; 
            width: 100%;
        }
    </style>
    <title>Accessory Products</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-black navbar-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Home</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="collapsibleNavbar">
                <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown">Category</a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="/pc">PC</a></li>
                    <li><a class="dropdown-item" href="/laptop">Laptop</a></li>
                    <li><a class="dropdown-item" href="/accessory">Accessory</a></li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-link text-white" role="button">
                        <a class="nav-link" href="/contact">Contact Us</a>
                    </li>
                    <li class="nav-link text-white" role="button">
                        <a class="nav-link" href="/about">About Us</a>
                    </li>
                    <li class="nav-link text-white" role="button">
                        <a class="nav-link" href="/addProduct">Add Product</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <h2 class="text-center">Update Product</h2>
        <div class="mt-4">
            <% if (product) { %>
                <form action="/editProduct/<%= product.productId %>" method="POST">
                    <div class="row mb-3">
                        <label for="productName" class="col-sm-2 col-form-label">Product Name:</label>
                        <div class="col-sm-10">
                            <input type="text" class="form-control" id="productName" name="productName" value="<%= product.productName %>" required>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <label for="price" class="col-sm-2 col-form-label">Price:</label>
                        <div class="col-sm-10">
                            <input type="text" class="form-control" id="price" name="price" min="0" step="0.01" value="<%= product.price %>" required>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <label for="description" class="col-sm-2 col-form-label">Description:</label>
                        <div class="col-sm-10">
                            <textarea class="form-control" id="description" name="description" rows="5" required><%= product.description %></textarea>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <label for="category" class="col-sm-2 col-form-label">Category:</label>
                        <div class="col-sm-10">
                            <!-- If product.category is 'pc', the expression evaluates to true and 'selected' is added to the <option> tag, making this option the default selected option.
                            If product.category is not 'pc', the expression evaluates to false and '' (an empty string) is added to the <option> tag, meaning this option is not selected by default. -->
                            <select class="form-select" id="category" name="category" required>                               
                                <option value="pc" <%= product.category === 'pc' ? 'selected' : '' %>>PC</option>
                                <option value="laptop" <%= product.category === 'laptop' ? 'selected' : '' %>>Laptop</option>
                                <option value="accessory" <%= product.category === 'accessory' ? 'selected' : '' %>>Accessory</option>
                            </select>
                        </div>
                    </div>
                    <div class="text-center">
                        <button type="submit" class="btn btn-primary">Update Product</button>
                    </div>
                </form>
            <% } else { %>
                <p>No product found.</p>
            <% } %>
            <div class="text-center mt-3">
                <a href="/" class="btn btn-secondary">Back</a>
            </div>
        </div>
    </div>
</body>
</html>
